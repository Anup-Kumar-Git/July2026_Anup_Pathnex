# DAY 13- CODING PRACTICE L1---------------------------------------

 # DEBUG DAY

	🔹 Ansible (broken)
	
	- nmae: Install nginx
	  hosts: all
	 tasks:
		- name: Install Nginx
		  yum :
			 name: nginx
			state: present



	🔹 Terraform (broken)

	resource "aws_instance" "BadEC2" {
		ami     ="ami-123"
		instance_type = "c5.large"
	
		tags = {
			name= "Broken"
		}
	}
	

	
	🔹Kubernetes ( Broken)

 	apiVersion : apps/v1
	kind :Deployment
	metadata:
		name: broken
	spec:
		replicas:2
		selector: 
			matchLables:
				app :broken
		template:
			metadata:
				lables:
					app: broken
			spec:
				containers:
					- name:app
					  image: nginx




# Matrix Builds


	🔹 Jenkins Pipeline — Matrix Build					 

	pipeline {
		agent any 
		enivornrment {
			INSTITUTE_NAME= "Pathnex"
		}

		stages {
			stage('Matrix Build') {
				matrix {
					axes {
						axis {
							name 'JAVA_VERSION'
							values '8', '11', '17'
						}
					}
					stages {
						stage('Build') {
							steps{
								sh "echo Building with Java $ {JAVS_VERSION}"
							}
						}
					}
				}	
			}
		}
	}




	🔹 GitLab CI — Matrix Jobs

	stages :
		- build
	
	build:
		stage:build
		image: maven: 3.8.1-jdk-17

		parallel :
			matrix:
				-JAVA_VERSION : ["8", "11", "17"]
		script:
			- echo "Building with java $JAVA_VERSION"



 
