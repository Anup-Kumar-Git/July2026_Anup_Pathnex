# DAY 09 - CODING PRACTICE L1-----------------------------------------

  #Templates, Internet Gateway, ConfigMap, CPU Load

	#🔹 Ansible — Use Jinja2 Template

	---
	  - name: Deploy template for Pathnex
	    hosts: all
	    become: yes


	tasks:
	   - name: Copy template file
	     template:
			src:pathenx.conf.j2
			dest: /etc/pathnex.conf


	🔹 Terraform — Internet Gateway

	resource "aws_intenent_gateway" "PathnexIGW" {
		vpc_id =aws_vpc.PathnexVPC.id

		tags ={
			Name ="Pathnex-IGW"
		}
	}



	🔹 Kubernetes — Create ConfigMap

	apiVersion : v1
	kind: cofigMap
	metadata:
		name:pathnex-config
	data:
		APP_MODE: "production"
		WELCOME_MSG: "Hello Pathnex"



	🔹 Shell Script — Check CPU Load

	#!/bin/bash

	LOAD=$(uptime | awk '{print $10}')

	echo"Current CPU Load: $LOAD"



# Artifacts and Archiving-----------------------------

	🔹 Jenkins Pipeline — Archive Artifacts

	pileline [
	agent any 
	environment {
		INSTITUTE_NAME= "pathnex"
	}
	stages {
		stage('Checkout') {
			steps{
				git url: 'https://github.com/Pathnex/sample-java-app.git'
			}
		}
		stage ('Build') {
			steps {
				sh 'mvn clean package'
			}
		}
		stage('Archive') {
			steps {
				archieveAreifacts artifacts: 'targer/*.jar', fingerprint:true
			}
		}
	}



	🔹 GitLab CI — Artifacts

	sages:
	   -build

	buid:
		stage: build
		image:mavan:3.8.1-jdk-17
		script:
			- git clone https://github.com/Pathnex/sample-java-app.git
			- cd sample-java-app
			- mvn clean Package
		artifacts:
			paths:
				- target/*.jar
			expire_in: 1 week



